<Window x:Class="CGA_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CGA_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

        <Grid Background="#FFF0EFB4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition  />
            <RowDefinition  Height="0.5*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Grid.ColumnSpan="2" >
            <TextBlock>Welcome to CGA!</TextBlock>
        </DockPanel>

        <DockPanel Grid.Row="1" Grid.Column="0" >
            <Button x:Name="button_login" Content="Login"></Button>
        </DockPanel>

        <DockPanel Grid.Row="1" Grid.Column="1" >
            <Button x:Name="button_register" Content="Register"></Button>
        </DockPanel>
    </Grid>
</Window>
